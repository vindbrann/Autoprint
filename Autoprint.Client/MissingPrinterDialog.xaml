<Window x:Class="Autoprint.Client.MissingPrinterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Imprimante Manquante" Height="220" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/Autoprint.ico"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Border CornerRadius="8" Background="{DynamicResource MaterialDesignPaper}" 
            BorderBrush="{DynamicResource PrimaryHueMidBrush}" BorderThickness="1"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
                <materialDesign:PackIcon Kind="PrinterAlert" Width="30" Height="30" 
                                         Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center"/>
                <TextBlock Text="Favori introuvable" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                           Foreground="{DynamicResource MaterialDesignBody}"
                           Margin="15,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="TxtMessage" Grid.Row="1" TextWrapping="Wrap" 
                       Foreground="{DynamicResource MaterialDesignBody}"
                       FontSize="14" VerticalAlignment="Center">
                L'imprimante...
            </TextBlock>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Oublier ce favori" IsCancel="True" Click="BtnOublier_Click"
                        Style="{StaticResource MaterialDesignFlatButton}" Foreground="OrangeRed" Margin="0,0,10,0"/>

                <Button Content="Réinstaller" IsDefault="True" Click="BtnReinstaller_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>